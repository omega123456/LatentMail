[alias]
gm-test-integration = "nextest run --features test-utils --test sync_lazy_body_integration --test reconcile_diff_integration --test reconcile_labels_integration --test mutations_star_integration --test mutations_coalescing_integration --test mutations_triage_integration --test ipc_contract_integration --test storage_schema_integration --test storage_history_gate_integration --test storage_concurrency_integration --test settings_persistence_integration --test settings_service_integration --test queue_lanes_integration --test queue_retry_integration --test queue_persistence_integration --test queue_traversal_lane_integration --test logging_writer_integration --test sanitize_allowlist_integration --test sanitize_images_integration --test auth_service_integration --test gmail_client_integration --test gmail_payload_mapping_integration --test gmail_backoff_integration --test sync_initial_integration --test sync_history_integration --test sync_resync_integration --test sync_threads_integration --test gmail_labels_integration --test traversal_cursor_integration --test traversal_quota_integration --test compose_mime_integration --test contacts_learning_integration --test compose_context_integration --test compose_staging_integration --test gmail_drafts_integration --test sync_materialize_integration --test compose_drafts_integration --test shell_prevent_default_integration --test avatars_cache_integration --test avatars_image_integration --test avatars_bimi_integration --test avatars_resolver_integration --test avatars_profile_integration --test thread_identity_integration --test avatars_service_integration"
gm-llvm-cov = "llvm-cov nextest --features test-utils --test sync_lazy_body_integration --test reconcile_diff_integration --test reconcile_labels_integration --test mutations_star_integration --test mutations_coalescing_integration --test mutations_triage_integration --test ipc_contract_integration --test storage_schema_integration --test storage_history_gate_integration --test storage_concurrency_integration --test settings_persistence_integration --test settings_service_integration --test queue_lanes_integration --test queue_retry_integration --test queue_persistence_integration --test queue_traversal_lane_integration --test logging_writer_integration --test sanitize_allowlist_integration --test sanitize_images_integration --test auth_service_integration --test gmail_client_integration --test gmail_payload_mapping_integration --test gmail_backoff_integration --test sync_initial_integration --test sync_history_integration --test sync_resync_integration --test sync_threads_integration --test gmail_labels_integration --test traversal_cursor_integration --test traversal_quota_integration --test compose_mime_integration --test contacts_learning_integration --test compose_context_integration --test compose_staging_integration --test gmail_drafts_integration --test sync_materialize_integration --test compose_drafts_integration --test shell_prevent_default_integration --test avatars_cache_integration --test avatars_image_integration --test avatars_bimi_integration --test avatars_resolver_integration --test avatars_profile_integration --test thread_identity_integration --test avatars_service_integration --fail-under-lines 90 --fail-under-functions 90 --fail-under-regions 80"

# Sign every locally run binary with a stable identity — see scripts/codesign-dev.sh.
# macOS only; Windows and CI never see this section.
[target.aarch64-apple-darwin]
runner = "scripts/codesign-dev.sh"

[target.x86_64-apple-darwin]
runner = "scripts/codesign-dev.sh"
