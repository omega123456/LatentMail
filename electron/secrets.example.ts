/**
 * Google OAuth 2.0 client ID and client secret for the Desktop app OAuth client.
 *
 * WHERE TO FIND THEM:
 *   Google Cloud Console → APIs & Services → Credentials
 *   → OAuth 2.0 Client IDs → your Desktop app client
 *   → Client ID and Client secret
 *
 * HOW TO USE:
 *   This file is the committed template. `electron/secrets.ts` is the git-ignored
 *   file that holds your real values. It is auto-created by `yarn install`
 *   (via the `postinstall` script) with empty placeholders — replace them with
 *   the real values from Google Cloud Console.
 *
 *   DO NOT put real values in this file — it is committed to git.
 */
export const GOOGLE_CLIENT_ID = '';
export const GOOGLE_CLIENT_SECRET = '';
